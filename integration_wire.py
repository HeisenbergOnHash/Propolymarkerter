# INTEGRATION WIRE (verified local only)
# Uses openrouter (free tier available) for analysis; polymarket adapter wired
# PaperBetRepository + agent pipeline connected; no live trade executed
def run_full_pipeline(): return 'paper_trading_integration_complete_local'
